"use client";
import {useEffect,useMemo,useState} from "react";import Link from "next/link";
type Profile={atsScore:number;candidate:{headline:string|null;name?:string|null;location?:string|null};summary?:string;skills:string[];targetRoles:string[];experience?:{role:string;highlights:string[]}[]};type Preferences={roles:string;locations:string;workMode:string;seniority:string;minSalary:string;keywords:string};type Job={id:string;title:string;company:string;location:string;mode:string;level:string;skills:string[];description:string;url:string;applyUrl?:string;source?:string;salary?:string};type Match={score:number;verdict:string;reasons:string[];matchedSkills:string[];missingSignals:string[];note:string};
function fallbackScore(job:Job,profile:Profile,prefs:Preferences){const text=`${job.title} ${job.company} ${job.location} ${job.mode} ${job.level} ${job.skills.join(" ")} ${job.description}`.toLowerCase();const all=[...profile.skills,...profile.targetRoles,prefs.roles,prefs.keywords].flatMap(x=>x.toLowerCase().split(/[^a-z0-9+#.-]+/)).filter(x=>x.length>2);const hits=all.filter(x=>text.includes(x));let score=45+Math.min(35,hits.length*5);if(prefs.locations&&prefs.locations.toLowerCase().split(/[,;]+/).some(x=>x.trim()&&job.location.toLowerCase().includes(x.trim())))score+=8;if(prefs.workMode!=="Any"&&job.mode.toLowerCase()===prefs.workMode.toLowerCase())score+=7;if(prefs.seniority!=="Any"&&job.level.toLowerCase().includes(prefs.seniority.split(" ")[0].toLowerCase()))score+=5;return Math.min(99,score)}
export default function JobsPage(){const[profile,setProfile]=useState<Profile|null>(null),[prefs,setPrefs]=useState<Preferences>({roles:"",locations:"",workMode:"Any",seniority:"Any",minSalary:"",keywords:""}),[jobs,setJobs]=useState<Job[]>([]),[loading,setLoading]=useState(false),[configured,setConfigured]=useState(false),[query,setQuery]=useState(""),[mode,setMode]=useState("All"),[matches,setMatches]=useState<Record<string,Match>>({}),[analyzing,setAnalyzing]=useState<string|null>(null),[error,setError]=useState(""),[saved,setSaved]=useState<Record<string,boolean>>({});useEffect(()=>{(async()=>{const r=await fetch("/api/profile");if(r.status===401){location.href="/login";return}const d=await r.json();if(d.profile)setProfile({atsScore:d.profile.atsScore,candidate:{headline:d.profile.headline,name:d.profile.user?.name,location:d.profile.location},summary:d.profile.summary,skills:d.profile.skills||[],targetRoles:d.profile.targetRoles||[],experience:d.profile.experience||[]});if(d.preferences)setPrefs(x=>({...x,...d.preferences}));})()},[]);useEffect(()=>{if(!profile)return;setLoading(true);fetch("/api/jobs").then(r=>r.json()).then(d=>{setJobs(d.jobs||[]);setConfigured(Boolean(d.configured))}).catch(()=>setError("Could not load job sources right now.")).finally(()=>setLoading(false))},[profile]);const ranked=useMemo(()=>jobs.map(job=>({job,score:matches[job.id]?.score??fallbackScore(job,profile!,prefs)})).filter(x=>(mode==="All"||x.job.mode.toLowerCase()===mode.toLowerCase())&&`${x.job.title} ${x.job.company} ${x.job.location} ${x.job.skills.join(" ")}`.toLowerCase().includes(query.toLowerCase())).sort((a,b)=>b.score-a.score),[jobs,profile,prefs,query,mode,matches]);async function analyze(job:Job){if(!profile)return;setAnalyzing(job.id);try{const r=await fetch("/api/match",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({profile,preferences:prefs,job})});const m=await r.json();if(r.ok)setMatches(x=>({...x,[job.id]:m}))}catch{}finally{setAnalyzing(null)}}async function saveJob(job:Job){setError("");const r=await fetch("/api/applications",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({job,status:"Saved"})});const d=await r.json();if(r.ok||r.status===200){setSaved(x=>({...x,[job.id]:true}))}else setError(d.error||"Could not save job.")}return <main className="analyze-page jobs-page"><nav className="nav shell"><Link className="brand" href="/"><span className="brand-mark">✦</span>JobPilot<span className="brand-ai">AI</span></Link><span className="analyze-nav-label">JOB MATCH INTELLIGENCE</span><div className="nav-actions"><Link className="nav-cta" href="/tracker">Tracker ↗</Link><Link className="nav-cta" href="/profile">Edit target ↗</Link></div></nav><section className="jobs-shell shell"><div className="jobs-header"><div><div className="eyebrow"><span className="pulse"/> Evidence-based matching</div><h1>Don't just find jobs.<br/><em>Know which ones fit.</em></h1><p>JobPilot compares listings with your real career profile and preferences, then explains the fit, missing signals and whether the opportunity is worth pursuing.</p></div><div className="match-summary"><strong>{ranked.length}</strong><span>{configured?"live listings":"jobs available"}</span></div></div>{!profile?<div className="empty-profile"><h2>Build your career profile first.</h2><p>Your profile is the evidence layer behind every recommendation.</p><Link className="button primary" href="/profile">Build my profile ↗</Link></div>:<>{!configured&&!loading&&<div className="source-notice"><b>Live job discovery is ready.</b><span>Add public Lever company slugs to <code>JOBPILOT_LEVER_COMPANIES</code> to activate listings.</span></div>}{error&&<div className="source-notice error">{error}</div>}<div className="job-controls"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search title, skill or company…"/><div className="filter-pills">{["All","Remote","Hybrid","On-site"].map(x=><button className={mode===x?"active":""} key={x} onClick={()=>setMode(x)}>{x}</button>)}</div></div>{loading?<div className="empty-profile"><h2>Finding live opportunities…</h2><p>Fetching published jobs from your configured sources.</p></div>:<div className="job-list">{ranked.map(({job,score})=>{const m=matches[job.id];return <article className="job-card" key={job.id}><div className="job-card-main"><div className="company-icon">{job.company[0]?.toUpperCase()}</div><div><div className="job-meta">{job.company} · {job.location} · {job.source||"Job source"}</div><h2>{job.title}</h2><p>{job.description.replace(/<[^>]+>/g," ").slice(0,280)}{job.description.length>280?"…":""}</p><div className="job-tags">{job.skills.slice(0,5).map(s=><span key={s}>{s}</span>)}<span>{job.mode}</span><span>{job.level}</span>{job.salary&&<span>{job.salary}</span>}</div></div></div><div className="job-match"><strong>{score}%</strong><small>{m?.verdict||"MATCH"}</small><button className="button secondary" disabled={saved[job.id]} onClick={()=>saveJob(job)}>{saved[job.id]?"Saved ✓":"Save job"}</button><Link className="button primary" href={`/tailor?job=${encodeURIComponent(job.id)}`}>Tailor ↗</Link>{!m&&<button className="button secondary" disabled={analyzing===job.id} onClick={()=>analyze(job)}>{analyzing===job.id?"Analyzing…":"Why this match ↗"}</button>}{m&&<a className="button secondary" href={job.url||job.applyUrl||"#"} target="_blank" rel="noreferrer">View opportunity ↗</a>}</div>{m&&<div className="match-insight"><div><b>Why it fits</b>{m.reasons.map(x=><span key={x}>✓ {x}</span>)}</div><div><b>Matched evidence</b><span>{m.matchedSkills.length?m.matchedSkills.join(" · "):"No strong keyword evidence"}</span></div><div><b>Missing signals</b><span>{m.missingSignals.length?m.missingSignals.join(" · "):"No obvious gaps from supplied data"}</span></div><p>{m.note}</p></div>}</article>})}{!ranked.length&&<div className="empty-profile"><h2>No matching listings found.</h2><p>{configured?"Try broader filters or configure another public job source.":"Activate a public Lever source to discover real jobs."}</p></div>}</div>}</>}</section><footer className="analyze-footer shell"><span>✦ JobPilot AI</span><span>Job match intelligence</span><span>Real evidence. No hiring guarantees.</span></footer></main>}
