import { NextResponse } from "next/server";
import { getCurrentUser } from "@/lib/auth";
import { safeHttpUrl, stringArrayField, stringField, optionalStringField } from "@/lib/validate";
import { rateLimit, rateLimitResponse } from "@/lib/rate-limit";
import { prisma } from "@/lib/prisma";

const ALLOWED = ["Saved", "Preparing", "Applied", "Interview", "Offer", "Rejected"] as const;
const INITIAL_ALLOWED = ["Saved", "Preparing", "Applied"] as const;
const RANK: Record<string, number> = { Saved: 0, Preparing: 1, Applied: 2, Interview: 3, Offer: 4, Rejected: 5 };
const MAX_APPLICATIONS_READ = 100;
const MAX_BODY_BYTES = 128 * 1024;
class RequestBodyTooLarge extends Error {}
function guard(userId: string, action: string) { return rateLimitResponse(rateLimit(`applications:${action}:${userId}`, action === "read" ? 60 : 30, 60_000)); }
function bodyTooLarge(req: Request) { const length = req.headers.get("content-length"); return length !== null && Number.isFinite(Number(length)) && Number(length) > MAX_BODY_BYTES; }
async function readJsonBody(req: Request) { const text = await req.text(); if (Buffer.byteLength(text, "utf8") > MAX_BODY_BYTES) throw new RequestBodyTooLarge(); return JSON.parse(text); }
function originViolation(req: Request) { const origin = req.headers.get("origin"); if (!origin) return false; try { const expected = new URL(process.env.NEXT_PUBLIC_APP_URL || "http://localhost:3000").origin; return new URL(origin).origin !== expected; } catch { return true; } }
function pageParam(value: string | null) { const page = Number(value ?? "1"); return Number.isInteger(page) && page >= 1 && page <= 1000 ? page : null; }
export async function GET(req: Request) { const user = await getCurrentUser(); if (!user) return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); const limited = guard(user.id, "read"); if (limited) return limited; const page = pageParam(new URL(req.url).searchParams.get("page")); if (page === null) return NextResponse.json({ error: "Invalid page." }, { status: 400 }); const applications = await prisma.application.findMany({ where: { userId: user.id }, include: { job: true, tailoredApplication: true }, orderBy: { updatedAt: "desc" }, skip: (page - 1) * MAX_APPLICATIONS_READ, take: MAX_APPLICATIONS_READ + 1 }); const hasMore = applications.length > MAX_APPLICATIONS_READ; if (hasMore) applications.pop(); return NextResponse.json({ applications, page, limit: MAX_APPLICATIONS_READ, hasMore }, { headers: { "Cache-Control": "private, no-store, max-age=0" } }); }
export async function POST(req: Request) { const user = await getCurrentUser(); if (!user) return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); const limited = guard(user.id, "write"); if (limited) return limited; if (originViolation(req)) return NextResponse.json({ error: "Invalid request origin." }, { status: 403 }); if (bodyTooLarge(req)) return NextResponse.json({ error: "Request body is too large." }, { status: 413 }); try { const b = await readJsonBody(req); if (!b || typeof b !== "object" || !b.job || typeof b.job !== "object") return NextResponse.json({ error: "Job details are required." }, { status: 400 }); const rawJob = b.job as Record<string, unknown>; const id = stringField(rawJob.id, 300, "Job id"), title = stringField(rawJob.title, 500, "Job title"), company = stringField(rawJob.company, 300, "Company"); const location = optionalStringField(rawJob.location, 500, "Location") ?? "", mode = optionalStringField(rawJob.mode, 100, "Mode") ?? null, level = optionalStringField(rawJob.level, 100, "Level") ?? null, source = optionalStringField(rawJob.source, 100, "Source") ?? null, salary = optionalStringField(rawJob.salary, 500, "Salary") ?? null, description = optionalStringField(rawJob.description, 20000, "Description") ?? null; const url = rawJob.url ? safeHttpUrl(rawJob.url, "Job URL") : null; const skills = rawJob.skills === undefined ? [] : stringArrayField(rawJob.skills, 100, 200, "Skills"); if (b.status && !ALLOWED.includes(b.status)) return NextResponse.json({ error: "Invalid status." }, { status: 400 }); const notes = b.notes === undefined ? "" : stringField(b.notes, 10000, "Notes"); const existingJob = await prisma.job.findUnique({ where: { id } }); const job = existingJob ?? await prisma.job.create({ data: { id, title, company, location, mode, level, source, salary, url, description, skills } }); const existing = await prisma.application.findUnique({ where: { userId_jobId: { userId: user.id, jobId: job.id } } }); const requested = b.status ? String(b.status) : "Saved"; if (!existing && !INITIAL_ALLOWED.includes(requested as (typeof INITIAL_ALLOWED)[number])) return NextResponse.json({ error: "New applications may start only at Saved, Preparing, or Applied." }, { status: 400 }); if (existing?.status === "Rejected" && requested !== "Rejected") return NextResponse.json({ error: "Rejected applications cannot be reopened through this action." }, { status: 409 }); const status = existing && RANK[existing.status] > RANK[requested] ? existing.status : requested; const application = await prisma.application.upsert({ where: { userId_jobId: { userId: user.id, jobId: job.id } }, create: { userId: user.id, jobId: job.id, status, notes, appliedAt: status === "Applied" ? new Date() : undefined }, update: { status, notes: b.notes === undefined ? undefined : notes, appliedAt: status === "Applied" && !existing?.appliedAt ? new Date() : undefined }, include: { job: true, tailoredApplication: true } }); return NextResponse.json({ application }, { headers: { "Cache-Control": "private, no-store, max-age=0" } }); } catch (error) { return NextResponse.json({ error: error instanceof RequestBodyTooLarge ? "Request body is too large." : "Could not save application." }, { status: error instanceof RequestBodyTooLarge ? 413 : 400 }); } }
export async function PATCH(req: Request) { const user = await getCurrentUser(); if (!user) return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); const limited = guard(user.id, "write"); if (limited) return limited; if (originViolation(req)) return NextResponse.json({ error: "Invalid request origin." }, { status: 403 }); if (bodyTooLarge(req)) return NextResponse.json({ error: "Request body is too large." }, { status: 413 }); try { const b = await readJsonBody(req); if (!b || typeof b !== "object") return NextResponse.json({ error: "Invalid request." }, { status: 400 }); const id = stringField(b.id, 300, "Application id"); if (b.status && !ALLOWED.includes(b.status)) return NextResponse.json({ error: "Invalid status." }, { status: 400 }); const notes = b.notes === undefined ? undefined : stringField(b.notes, 10000, "Notes"); const existing = await prisma.application.findFirst({ where: { id, userId: user.id } }); if (!existing) return NextResponse.json({ error: "Application not found." }, { status: 404 }); if (existing.status === "Rejected" && b.status && b.status !== "Rejected") return NextResponse.json({ error: "Rejected applications cannot be reopened." }, { status: 409 }); const nextStatus = b.status || existing.status; if (RANK[existing.status] > RANK[nextStatus]) return NextResponse.json({ error: "Application status cannot move backwards." }, { status: 409 }); const application = await prisma.application.update({ where: { id: existing.id }, data: { status: b.status || undefined, notes, appliedAt: nextStatus === "Applied" && !existing.appliedAt ? new Date() : undefined }, include: { job: true, tailoredApplication: true } }); return NextResponse.json({ application }, { headers: { "Cache-Control": "private, no-store, max-age=0" } }); } catch (error) { return NextResponse.json({ error: error instanceof RequestBodyTooLarge ? "Request body is too large." : "Could not update application." }, { status: error instanceof RequestBodyTooLarge ? 413 : 400 }); } }
export async function DELETE(req: Request) { const user = await getCurrentUser(); if (!user) return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); const limited = guard(user.id, "write"); if (limited) return limited; if (originViolation(req)) return NextResponse.json({ error: "Invalid request origin." }, { status: 403 }); if (bodyTooLarge(req)) return NextResponse.json({ error: "Request body is too large." }, { status: 413 }); try { const b = await readJsonBody(req); if (!b || typeof b !== "object" || !b.id) return NextResponse.json({ error: "Application id is required." }, { status: 400 }); const id = stringField(b.id, 300, "Application id"); const result = await prisma.application.deleteMany({ where: { id, userId: user.id } }); if (!result.count) return NextResponse.json({ error: "Application not found." }, { status: 404 }); return NextResponse.json({ ok: true }, { headers: { "Cache-Control": "private, no-store, max-age=0" } }); } catch (error) { return NextResponse.json({ error: error instanceof RequestBodyTooLarge ? "Request body is too large." : "Could not delete application." }, { status: error instanceof RequestBodyTooLarge ? 413 : 400 }); } }
