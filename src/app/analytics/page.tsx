"use client";

import Link from "next/link";
import { useEffect, useState } from "react";

type Analytics = { summary: { tracked:number; applied:number; interviews:number; offers:number; rejected:number; responseRate:number; interviewRate:number; offerRate:number; rejectionRate:number }; funnel:{label:string;value:number}[]; aging:{applicationId:string;title:string;company:string;status:string;days:number}[]; companies:{company:string;applications:number;interviews:number;offers:number}[]; insights:string[]; trend:{label:string;applications:number;applied:number}[] };

export default function AnalyticsPage(){
 const [data,setData]=useState<Analytics|null>(null),[error,setError]=useState("");
 useEffect(()=>{fetch("/api/analytics",{cache:"no-store"}).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error||"Analytics could not be loaded.");return d}).then(setData).catch(e=>setError(e instanceof Error?e.message:"Analytics could not be loaded."));},[]);
 if(error)return <main className="page-shell"><section className="panel"><p className="truth-note">{error}</p></section></main>;
 if(!data)return <main className="page-shell"><section className="panel"><p className="muted">Building your recruiting picture…</p></section></main>;
 const s=data.summary,max=Math.max(1,...data.trend.map(x=>Math.max(x.applications,x.applied)));
 return <main><nav className="nav shell"><Link className="brand" href="/dashboard"><span className="brand-mark">✦</span>JobPilot<span className="brand-ai">AI</span></Link><div className="nav-actions"><Link href="/dashboard">Command center</Link><Link className="nav-cta" href="/tracker">Tracker ↗</Link></div></nav><section className="analytics-shell shell"><div className="eyebrow"><span className="pulse"/> Search intelligence</div><div className="analytics-header"><div><h1>Know what is <em>working.</em></h1><p>Turn your application history into practical signals — conversion, response, aging, and where to focus next.</p></div></div><div className="analytics-metrics"><Metric label="Tracked" value={s.tracked}/><Metric label="Applied" value={s.applied}/><Metric label="Interview rate" value={`${s.interviewRate}%`}/><Metric label="Response rate" value={`${s.responseRate}%`}/><Metric label="Offers" value={s.offers}/></div><section className="analytics-card"><div className="kicker">ACTIVITY TREND</div><h2>Application momentum</h2><p className="truth-note">Weekly activity based on when roles entered your tracker.</p><div className="trend-chart">{data.trend.map(x=><div className="trend-column" key={x.label}><div className="trend-values"><b>{x.applications}</b>{x.applied>0&&<span>{x.applied} applied</span>}</div><div className="trend-bars"><i style={{height:`${Math.max(4,x.applications/max*100)}%`}}/><i style={{height:`${Math.max(4,x.applied/max*100)}%`}}/></div><small>{x.label}</small></div>)}</div></section><div className="analytics-grid"><section className="analytics-card analytics-funnel"><div className="kicker">PIPELINE FUNNEL</div><h2>Where applications convert</h2>{data.funnel.map(x=><div className="funnel-row" key={x.label}><span>{x.label}</span><div className="funnel-track"><i style={{width:`${data.funnel[0].value?Math.max(4,x.value/data.funnel[0].value*100):0}%`}}/></div><b>{x.value}</b></div>)}<p className="truth-note">Rates are directional, not a promise of future outcomes.</p></section><section className="analytics-card"><div className="kicker">SIGNAL</div><h2>What the data says</h2><div className="insight-list">{data.insights.map((x,i)=><div className="insight" key={i}><b>0{i+1}</b><span>{x}</span></div>)}</div></section></div><div className="analytics-grid"><section className="analytics-card"><div className="kicker">AGING</div><h2>Roles that need attention</h2>{data.aging.length?<div className="analytics-list">{data.aging.map(x=><Link className="analytics-row" href={`/application/${encodeURIComponent(x.applicationId)}`} key={x.applicationId}><div><strong>{x.title}</strong><span>{x.company} · {x.status}</span></div><b>{x.days}d</b></Link>)}</div>:<p className="truth-note">No open applications need attention yet.</p>}</section><section className="analytics-card"><div className="kicker">COMPANIES</div><h2>Where you are concentrating</h2>{data.companies.length?<div className="analytics-list">{data.companies.map(x=><div className="analytics-row" key={x.company}><div><strong>{x.company}</strong><span>{x.applications} tracked · {x.interviews} interview · {x.offers} offer</span></div><b>{x.applications}</b></div>)}</div>:<p className="truth-note">Company patterns appear after you track roles.</p>}</section></div></section></main>
}
function Metric({label,value}:{label:string;value:number|string}){return <div><small>{label}</small><b>{value}</b></div>}
