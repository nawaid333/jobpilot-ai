import Link from "next/link";
import { notFound, redirect } from "next/navigation";
import { getCurrentUser } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
import ApplicationActions from "./actions";

export default async function ApplicationPage({params}:{params:Promise<{id:string}>}){
 const user=await getCurrentUser(); if(!user)redirect("/login"); const {id}=await params;
 const app=await prisma.application.findFirst({where:{id,userId:user.id},include:{job:true,tailoredApplication:true}}); if(!app)notFound();
 const signals=await prisma.emailSignal.findMany({where:{userId:user.id,applicationId:app.id},orderBy:{createdAt:"desc"},take:10}); const tailored=app.tailoredApplication;
 return <main className="analyze-page application-page"><nav className="nav shell"><Link className="brand" href="/"><span className="brand-mark">✦</span>JobPilot<span className="brand-ai">AI</span></Link><span className="analyze-nav-label">APPLICATION WORKSPACE</span><div className="nav-actions"><Link href="/signals">Gmail Signals ↗</Link><Link className="nav-cta" href="/tracker">Tracker ↗</Link></div></nav><section className="tracker-shell shell"><div className="jobs-header"><div><div className="eyebrow"><span className="pulse"/> Application workspace</div><h1>{app.job.title}<br/><em>{app.job.company}</em></h1><p>{app.job.location}{app.job.mode?` · ${app.job.mode}`:""}{app.job.level?` · ${app.job.level}`:""}</p></div><div className="match-summary"><strong>{app.status}</strong><span>current status</span></div></div><ApplicationActions id={app.id} status={app.status} notes={app.notes||""}/><div className="dashboard-grid"><div className="dashboard-card dashboard-feature"><span>JOB DETAILS</span><h2>{app.job.title}</h2><p>{app.job.description?.replace(/<[^>]+>/g," ").slice(0,900)||"No job description saved."}</p><div className="job-tags">{app.job.skills.map(s=><span key={s}>{s}</span>)}</div>{app.job.url&&<a className="button secondary" href={app.job.url} target="_blank" rel="noreferrer">Open original listing ↗</a>}</div><div className="dashboard-card"><span>APPLICATION STATUS</span><h2>{app.status}</h2><p>Created {new Date(app.createdAt).toLocaleDateString()} · Updated {new Date(app.updatedAt).toLocaleDateString()}</p><p>{app.appliedAt?`Applied ${new Date(app.appliedAt).toLocaleDateString()}`:"Not marked as applied yet."}</p></div></div>{tailored?<div className="dashboard-recent"><div className="preference-heading"><div><div className="kicker">TAILORED PACKAGE</div><h2>Ready for review.</h2></div><div><Link className="button primary" href={`/tailor?job=${encodeURIComponent(app.id)}`}>Open tailoring ↗</Link>{app.status!=="Applied"&&<Link className="button secondary" href={`/tailor?job=${encodeURIComponent(app.id)}&action=apply`}>Review & apply →</Link>}</div></div><div className="recent-list"><div className="recent-row"><div><strong>Tailored summary</strong><span>{tailored.tailoredSummary}</span></div></div><div className="recent-row"><div><strong>Recommendation</strong><span>{tailored.recommendation}</span></div></div>{tailored.missingRequirements.length>0&&<div className="recent-row"><div><strong>Missing requirements</strong><span>{tailored.missingRequirements.join(" · ")}</span></div></div>}</div><div className="tracker-tailored"><strong>Cover letter prepared</strong><span>{tailored.coverLetter.slice(0,700)}{tailored.coverLetter.length>700?"…":""}</span></div></div>:<div className="dashboard-recent"><div className="preference-heading"><div><div className="kicker">TAILORING</div><h2>Prepare this application.</h2></div><Link className="button primary" href={`/tailor?job=${encodeURIComponent(app.id)}`}>Tailor application ↗</Link></div><p className="truth-note">Create a tailored package from your verified career profile before you apply.</p></div>}<div className="dashboard-recent"><div className="preference-heading"><div><div className="kicker">GMAIL SIGNALS</div><h2>{signals.length?`${signals.length} linked recruiting signal${signals.length===1?"":"s"}`:"No linked signals yet"}</h2></div><Link className="button secondary" href="/signals">Review signals →</Link></div>{signals.length?<div className="recent-list">{signals.map(s=><div className="recent-row" key={s.id}><div><strong>{s.subject||"Recruiting email"}</strong><span>{s.from} · {s.suggestedStatus||"Signal"}</span></div><b>{s.applied?"Applied":"Detected"}</b></div>)}</div>:<p className="truth-note">Recruiter emails linked to this application will appear here.</p>}</div><div className="dashboard-recent"><div className="preference-heading"><div><div className="kicker">NEXT STEP</div><h2>Keep the application moving.</h2></div><Link className="button secondary" href="/interview">Interview Coach →</Link></div><p className="truth-note">When this application reaches an interview, use role-specific practice and evidence from your career profile.</p></div></section><footer className="analyze-footer shell"><span>✦ JobPilot AI</span><span>Application workspace</span><span>One application. Full context.</span></footer></main>;
}
